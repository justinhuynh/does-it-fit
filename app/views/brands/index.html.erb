<h3>Brands</h3>
<ul class="small-block-grid-3 medium-block-grid-4 large-block-grid-5">
  <% @brands.each do |brand| %>
    <li>
      <%= link_to brand_path(brand) do %>
        <img src="<%= brand.products.sample.image_url %>">
        <center><%= brand.name %></center>
      <% end %>
    </li>
  <% end %>
</ul>
