<%= @current_user  %>
<% if @products.empty? %>
  <p>There are no posts containing the term(s) <%= params[:search] %>.</p>
<% end %>

<h3>Products in <%= @category.name %></h3>
<%= render '/layouts/show_products', products: @products %>
